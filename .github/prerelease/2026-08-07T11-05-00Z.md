<!-- prerelease: timestamp=2026-08-07T11-05-00Z repo=danieljustus/symaira-print head=7c596b4f4a5eec23536546911b8f8403733dedf2 target=v0.5.0 bump=minor gate=pass -->

# Prerelease Gate — 2026-08-07

## Summary
- Repo: danieljustus/symaira-print
- HEAD: `7c596b4f4a5eec23536546911b8f8403733dedf2`
- Last release: v0.4.0 (2026-08-05)
- Proposed target: **v0.5.0** (bump: minor — two `feat` PRs in the unreleased diff: #113 macOS Finder integration, #114 accessible control names + profile preselection)
- Gate: **pass**

## Gate checks
| Check | Result | Detail |
|---|---|---|
| CI / tests (HEAD) | PASS | CI run 31172006523 success + CodeQL 31172004862 success on HEAD 7c596b4; local full tier green (go test ./..., make lint, make build). Cancelled run 31171988567 is on older SHA a999af9 (superseded) — not a failure |
| Version consistency | PASS | 3 mentions corrected to v0.5.0 (README scaffold note, client/project.yml MARKETING_VERSION, cmd/symprint/main.go version). release.yml comment `(v0.4.0, run 30999935597)` is historical pin documentation — intentionally not bumped |
| README / doc currency | PASS | README already documents Finder integration, DMG install and lsregister re-registration (#113); install commands (`go install …@latest`, `brew install danieljustus/tap/symprint`) correct; no stale feature claims found |
| Coverage vs threshold | PASS | lines 87.1% / threshold 80% (default — no repo hard gate; warn-only source) |
| Coverage regression | PASS | baseline 86.3% (v0.4.0) → 87.1% (delta +0.8) |
| Patch coverage | PASS | n/a — no production Go statements in `v0.4.0..HEAD` (test additions + Swift client only) |
| Assets preview | PASS | expected matrix: 6 archives + checksums.txt + Symprint.dmg (matches v0.4.0 release exactly); GoReleaser pinned v2.17.1 = binary of last successful run |
| Distribution publishers | PASS | CLI Formula: `.goreleaser.yml` `brews:` → `danieljustus/homebrew-tap/Formula/symprint.rb`, token `HOMEBREW_TAP_GITHUB_TOKEN` present; GUI: `release-client` job (macos-15) builds/signs/notarizes/staples DMG and uploads it. All secrets present: CERTIFICATE_P12, CERTIFICATE_PASSWORD, NOTARY_API_KEY/ID/ISSUER, HOMEBREW_TAP_GITHUB_TOKEN. No Cask configured for symprint — repo convention is DMG-on-GitHub-release (README §Installing the app) |
| Security | PASS | 0 open Dependabot alerts, 0 open code-scanning alerts |

## Findings
- [ ] **[WARN][Version] Milestone title v0.4.1 predates the computed minor bump**
  - **Status quo:** Open milestone `v0.4.1` (7 closed issues #106–#112, 0 open) — planned as a patch release before #113/#114 merged as `feat`.
  - **Likely cause:** Milestone was titled during planning; the merged work contains two features, so the computed bump is minor (v0.5.0).
  - **Proposed solution:** Rename the milestone to `v0.5.0` during release reconciliation (07-gh-release step 13 renames open release milestones to the actual tag; all linked issues are closed, so it can also be closed after release).
  - **Handoff:** `/07-gh-release` (rename + closeout is part of its flow; no issue creation needed)

## Applied mechanical fixes (default mode)
- `README.md:57` scaffold note `v0.4.0` → `v0.5.0`
- `client/project.yml:13` `MARKETING_VERSION: "0.4.0"` → `"0.5.0"`
- `cmd/symprint/main.go:24` `var version = "0.4.0"` → `"0.5.0"`
(Uncommitted — rides into the release commit, per convention commit 1938d8d.)

## Release notes source (merged PRs since v0.4.0)
- #113 macOS app: Finder integration — document types, open handler, bundle identity (feat; closes #106–#112)
- #114 macOS client UX: accessible control names, single-prefix errors, frontmatter profile preselection (feat)
- #115 fix(client): consume external open events, accept all markdown extensions (fix)
- #118 ci: pin GoReleaser binary version to v2.17.1 (packaging)
- #119 test: cover asset-materialization failure paths (tests)

## Next step
- gate=pass → `/07-gh-release` (target v0.5.0, minor)
