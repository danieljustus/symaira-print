<!-- coverage: timestamp=2026-07-01T06:25:00Z repo=danieljustus/symaira-print head=d97ae534ad29ca1302b6f16251456a02355dfada overall=75.2% patch=n/a baseline=55.9% delta=+19.3% mutation=n/a assertdensity=n/a slowtests=0 suite=1.49 -->

# Coverage Review - 2026-07-01

## Summary
- Tooling: `go test` with `go tool cover` (Go 1.26.4)
- Command: `CGO_ENABLED=0 go test ./... -coverprofile=/tmp/coverage.out -covermode=atomic`
- Result: pass
- Overall: lines 75.2% (statements)
- Patch coverage: n/a — no diff against `main` (HEAD is on the default branch, working tree clean of code changes)
- Baseline: 55.9% → 75.2% (delta **+19.3%**) — baseline from `.github/coverage/2026-06-30T22-19-50Z.md`, head `417e0e3`, confirmed ancestor of current HEAD
- Mutation score: n/a — no mutation tool configured for Go in this repo, and no `--mutation` flag was given; skipped per rule 15
- Test quality: not re-scanned in depth this run (time-boxed for the release gate); spot check below
- Gate: no coverage threshold/gate configured in CI (no `fail_under`, no coverage-check step beyond veraPDF conformance) — this is an informational run, not a hard gate

## Metrics
| Area | Lines | Notes |
|---|---:|---|
| `internal/config` | 100.0% | fully covered |
| `internal/mcp` | 98.1% | fully covered |
| `internal/press` | 84.0% | strong; remaining gaps are minor error branches |
| `cmd/symprint` | 69.1% | up from 27.0% — doctor/profiles/config command tests landed (#44) |
| `internal/assets` | 0.0% (per-package, cross-package covered via `internal/press`) | `FS`/`TemplateNames` dead code removed (#46), no longer a gap |
| **Total** | **75.2%** | up from 55.9% baseline |

## Test quality
- **Mutation:** n/a — not run.
- **Assert density:** not re-measured; prior report found 0 zero-assert tests and this run's spot check of `internal/press::TestRenderTypstTimeout` shows asserted behavior, consistent with the prior finding.
- **AAA:** not re-scanned; no new systemic pattern observed in the fast diff since the prior run.
- **Performance:** suite wall time dropped from 6.12s to ~1.49s — `TestRenderTypstTimeout` (previously 5.11s, 84% of suite time) now completes in 0.11s after #45 (`exec sleep` fix). No test currently over the 0.5s threshold.

## Findings

All four findings from the prior report (`.github/coverage/2026-06-30T22-19-50Z.md`) are resolved by commits already on HEAD:
- `doctor`/`profiles`/`config`/`main.go` helpers are now 100% covered (`aa46366` #44).
- `TestRenderTypstTimeout` now runs in 0.11s, no longer dominating suite time (`6e852b4` #45).
- `internal/assets.FS` / `TemplateNames` dead code removed (`d97ae53` #46).

No new issue-worthy findings this run.

## Non-issues
- `internal/assets` per-package 0.0% — cross-package coverage via `internal/press` render tests remains the correct interpretation (see prior report's methodology note); the two previously-uncovered exported symbols were removed rather than tested, which is the right outcome per that finding's proposed solution.

## Next step
No coverage findings to plan. Overall coverage improved +19.3% since the last report with no regression.
